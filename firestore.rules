rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Click Club does not use Firestore yet.
    // Deny all reads and writes by default so the database
    // cannot be abused if it is provisioned on the project.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
